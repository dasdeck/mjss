import * as less from 'less';

describe('less tests', () => {
    it('less renders font-family', () => {

        // less.render('@color1: red;.tester2{color: @color1;}', (err, res) => {
        //     debugger
        // });
        // less.render('@font1:-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;.class1{font-family:@font1;}', (err, res) => {
        //     debugger
        // });
    });
})

